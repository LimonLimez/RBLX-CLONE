You have installed CMake and Visual Studio (Great!), but **Git** is missing from your system or PATH.

CMake uses Git to download the libraries (GLFW, GLM, etc.).

**Option A: Install Git (Recommended)**
1.  Download Git: [https://git-scm.com/download/win](https://git-scm.com/download/win)
2.  Install it (Standard options are fine).
3.  Restart Cursor/Terminal.

**Option B: I can switch to manual downloads**
I can rewrite the build script to download ZIP files instead of using Git, but this is more fragile.

**Please reply with "1" (I will install Git) or "2" (Rewrite script to use ZIPs).**










